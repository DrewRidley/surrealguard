{
  "name": "@surrealguard-example/sveltekit",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "SvelteKit / Svelte 5 SurrealGuard example: typed createLive + preload.",
  "scripts": {
    "generate": "surrealguard generate --out src/lib/surrealguard.generated.ts",
    "typecheck": "svelte-check --tsconfig ./tsconfig.json"
  },
  "dependencies": {
    "@surrealguard/client": "workspace:*",
    "@surrealguard/query": "workspace:*",
    "@surrealguard/svelte": "workspace:*",
    "surrealdb": "2.0.8"
  },
  "devDependencies": {
    "svelte": "^5.1.0",
    "typescript": "^5.6.0",
    "svelte-check": "^4.0.0"
  }
}
