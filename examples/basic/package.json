{
  "name": "@surrealguard-example/basic",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Vanilla-TS SurrealGuard example: surrealguard generate -> typed db.query.",
  "scripts": {
    "generate": "surrealguard generate --out surrealguard.generated.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@surrealguard/client": "workspace:*",
    "surrealdb": "2.0.8"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
