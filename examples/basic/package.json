{
  "name": "@surrealql-analyzer-example/basic",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Vanilla-TS SurrealQL Analyzer example: surrealql-analyzer generate -> typed defineQuery + db.run.",
  "scripts": {
    "generate": "surrealql-analyzer generate --out src/surrealql-analyzer.generated.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@surrealdb/analyzer-client": "workspace:*",
    "surrealdb": "2.0.8"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
