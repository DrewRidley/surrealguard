# Oracle baseline — every finding the real-world corpus produces, with a verdict.
#
# The corpus is NOT all-valid; it contains genuinely broken SurrealQL. A growing
# count is therefore not a failure. An UNTRIAGED finding is, and so is one that
# silently disappears.
#
# Verdicts:
#   genuine  — the corpus SurrealQL really is wrong (say how it was verified)
#   expected — correct behaviour on input we accept as-is
#   BUG      — a false positive. Any line with this verdict is a TODO on us.
#
# Regenerate with `scripts/oracle.py update` (existing verdicts are preserved).

# total: 43 finding(s)

E1001  schema/apps/commerce/discount.surql
    `record<loyalty_tier>` targets a table that's never defined
    verdict: genuine — the table is defined as `commerce_loyalty_tier` (loyalty_tier.surql:1); the `loyalty_*` names are stale post-rename references. No `DEFINE TABLE loyalty_tier` exists anywhere in the corpus.

E1001  schema/apps/commerce/loyalty/loyalty_reward.surql
    `record<loyalty_program>` targets a table that's never defined
    verdict: genuine — same stale rename: the file itself defines `commerce_loyalty_reward`, but its fields still point at `loyalty_program` / `loyalty_tier`.

E1001  schema/apps/commerce/loyalty/loyalty_reward.surql
    `record<loyalty_tier>` targets a table that's never defined
    verdict: genuine — same stale rename: the file itself defines `commerce_loyalty_reward`, but its fields still point at `loyalty_program` / `loyalty_tier`.

E1001  schema/apps/commerce/loyalty/loyalty_tier.surql
    `record<loyalty_program>` targets a table that's never defined
    verdict: genuine — same stale rename: defines `commerce_loyalty_tier`, references `record<loyalty_program>`.

E1001  schema/apps/commerce/loyalty/loyalty_transaction.surql
    `record<loyalty_program>` targets a table that's never defined
    verdict: genuine — same stale rename: three fields point at `loyalty_program` / `loyalty_reward` / `loyalty_tier`, all defined as `commerce_*`.

E1001  schema/apps/commerce/loyalty/loyalty_transaction.surql
    `record<loyalty_reward>` targets a table that's never defined
    verdict: genuine — same stale rename: three fields point at `loyalty_program` / `loyalty_reward` / `loyalty_tier`, all defined as `commerce_*`.

E1001  schema/apps/commerce/loyalty/loyalty_transaction.surql
    `record<loyalty_tier>` targets a table that's never defined
    verdict: genuine — same stale rename: three fields point at `loyalty_program` / `loyalty_reward` / `loyalty_tier`, all defined as `commerce_*`.

E1001  schema/apps/commerce/order/has_line_item.surql
    `record<kitchen_station>` targets a table that's never defined
    verdict: genuine — the table is `pos_kitchen_station` (pos_kitchen_station.surql:1); `kitchen_station` is never defined.

E1001  schema/apps/commerce/pos/kitchen/pos_kitchen_ticket.surql
    `record<kitchen_station>` targets a table that's never defined
    verdict: genuine — same file's own table is `pos_kitchen_station`; the ticket's `station` field still says `record<kitchen_station>`.

E1001  schema/apps/commerce/products/service.surql
    `record<product_kind>` targets a table that's never defined
    verdict: genuine — `product_kind` is defined nowhere in the corpus (nearest real tables are `product_category` / `product`).

E1001  schema/apps/crm/customer.surql
    `record<loyalty_program>` targets a table that's never defined
    verdict: genuine — `loyalty_program` / `loyalty_tier` are the pre-rename names; the real tables are `commerce_loyalty_program` / `commerce_loyalty_tier`.

E1001  schema/apps/crm/customer.surql
    `record<loyalty_tier>` targets a table that's never defined
    verdict: genuine — `loyalty_program` / `loyalty_tier` are the pre-rename names; the real tables are `commerce_loyalty_program` / `commerce_loyalty_tier`.

E1001  schema/communication/email_address.surql
    `mailbox_folder` is not a defined table
    verdict: genuine — `COMPUTED <~mailbox_folder` back-references a table that is never defined; the computed field can only ever be `[]`.

E1001  schema/organization/@functions.surql
    `has_billing_module` is not a defined table
    verdict: genuine — engine-verified: `SELECT * FROM <undefined table>` errors outright on SurrealDB 3.0.5 ("The table 'X' does not exist"). `fn::organization::modules` and `fn::organization::billing::usage` cannot run at all.

E1001  schema/organization/@functions.surql
    `organization_billing_limit` is not a defined table
    verdict: genuine — engine-verified: `SELECT * FROM <undefined table>` errors outright on SurrealDB 3.0.5 ("The table 'X' does not exist"). `fn::organization::modules` and `fn::organization::billing::usage` cannot run at all.

E1001  schema/organization/organization_delegation_grant.surql
    `record<user>` targets a table that's never defined
    verdict: genuine — `record<team | user | organization_unit>`; there is no `user` table in the corpus (accounts live in `account`).

E1001  schema/person/account.surql
    `passkey` is not a defined table
    verdict: genuine — `COMPUTED <~passkey` back-references a table that is never defined.

E1001  schema/suite/task/task.surql
    `task_comment` is not a defined table
    verdict: genuine — `COMPUTED <~task_comment` back-references a table that is never defined.

E1002  schema/auth/entity_access.surql
    `member_of` has no field `status`
    verdict: genuine — `member_of` is SCHEMAFULL with only `role` / `joined_at` (+ in/out). `WHERE ... AND status = 'active'` therefore never matches, so both team-membership permission branches are dead.

E1002  schema/auth/entity_access.surql
    `member_of` has no field `status`
    verdict: genuine — `member_of` is SCHEMAFULL with only `role` / `joined_at` (+ in/out). `WHERE ... AND status = 'active'` therefore never matches, so both team-membership permission branches are dead.

E1002  schema/organization/organization.surql
    `organization_unit` has no field `random`
    verdict: genuine — engine-verified: `random = 'test'` (organization.surql:78) on SCHEMAFULL `organization_unit` fails with "Found field 'random', but no such field exists". Leftover debug line; the setup_roles event throws on every organization CREATE.

E1002  schema/person/@function.surql
    `organization` has no field `slug`
    verdict: genuine — `organization` is SCHEMAFULL and has no `slug` field, so the username-vs-org-slug collision check at line 24 can never fire.

E1002  schema/suite/project/project.surql
    `task_status` has no field `category`
    verdict: genuine — engine-verified: `task_status` is SCHEMAFULL with template/name/description/color/state. `CREATE task_status SET ... category = ..., project = ...` fails with "Found field 'category', but no such field exists for table 'task_status'". The setup_statuses event is stale — projects now get statuses via `project_task_status`.

E1002  schema/suite/project/project.surql
    `task_status` has no field `category`
    verdict: genuine — engine-verified: `task_status` is SCHEMAFULL with template/name/description/color/state. `CREATE task_status SET ... category = ..., project = ...` fails with "Found field 'category', but no such field exists for table 'task_status'". The setup_statuses event is stale — projects now get statuses via `project_task_status`.

E1002  schema/suite/project/project.surql
    `task_status` has no field `category`
    verdict: genuine — engine-verified: `task_status` is SCHEMAFULL with template/name/description/color/state. `CREATE task_status SET ... category = ..., project = ...` fails with "Found field 'category', but no such field exists for table 'task_status'". The setup_statuses event is stale — projects now get statuses via `project_task_status`.

E1002  schema/suite/project/project.surql
    `task_status` has no field `category`
    verdict: genuine — engine-verified: `task_status` is SCHEMAFULL with template/name/description/color/state. `CREATE task_status SET ... category = ..., project = ...` fails with "Found field 'category', but no such field exists for table 'task_status'". The setup_statuses event is stale — projects now get statuses via `project_task_status`.

E1002  schema/suite/project/project.surql
    `task_status` has no field `category`
    verdict: genuine — engine-verified: `task_status` is SCHEMAFULL with template/name/description/color/state. `CREATE task_status SET ... category = ..., project = ...` fails with "Found field 'category', but no such field exists for table 'task_status'". The setup_statuses event is stale — projects now get statuses via `project_task_status`.

E1002  schema/suite/project/project.surql
    `task_status` has no field `project`
    verdict: genuine — engine-verified: `task_status` is SCHEMAFULL with template/name/description/color/state. `CREATE task_status SET ... category = ..., project = ...` fails with "Found field 'category', but no such field exists for table 'task_status'". The setup_statuses event is stale — projects now get statuses via `project_task_status`.

E1002  schema/suite/project/project.surql
    `task_status` has no field `project`
    verdict: genuine — engine-verified: `task_status` is SCHEMAFULL with template/name/description/color/state. `CREATE task_status SET ... category = ..., project = ...` fails with "Found field 'category', but no such field exists for table 'task_status'". The setup_statuses event is stale — projects now get statuses via `project_task_status`.

E1002  schema/suite/project/project.surql
    `task_status` has no field `project`
    verdict: genuine — engine-verified: `task_status` is SCHEMAFULL with template/name/description/color/state. `CREATE task_status SET ... category = ..., project = ...` fails with "Found field 'category', but no such field exists for table 'task_status'". The setup_statuses event is stale — projects now get statuses via `project_task_status`.

E1002  schema/suite/project/project.surql
    `task_status` has no field `project`
    verdict: genuine — engine-verified: `task_status` is SCHEMAFULL with template/name/description/color/state. `CREATE task_status SET ... category = ..., project = ...` fails with "Found field 'category', but no such field exists for table 'task_status'". The setup_statuses event is stale — projects now get statuses via `project_task_status`.

E1002  schema/suite/project/project.surql
    `task_status` has no field `project`
    verdict: genuine — engine-verified: `task_status` is SCHEMAFULL with template/name/description/color/state. `CREATE task_status SET ... category = ..., project = ...` fails with "Found field 'category', but no such field exists for table 'task_status'". The setup_statuses event is stale — projects now get statuses via `project_task_status`.

E2004  schema/organization/@functions.surql
    `=` can't combine a `array<bool>` and a `bool`
    verdict: genuine — engine-verified: `array::matches([1,2,3], 2)` returns `[false, true, false]`, an array<bool>. `fn::organization::modules::contain(...) = false` is therefore never true, so `fn::organization::modules::require` never THROWs — the module gate is dead.

E2004  schema/organization/employee_of.surql
    `=` can't combine a `array<{ count: int }>` and a `int`
    verdict: genuine — engine-verified: `SELECT count() FROM t GROUP ALL` returns `[{ count: N }]`, an array. `IF $admins = 0` is never true, so guard_last_admin never protects the last administrator.

E2034  schema/suite/project/project.surql
    `template` must be set when creating a `task_status`
    verdict: genuine — engine-verified: `template` is `TYPE record<task_status_template>` with no DEFAULT, so the CREATE fails with "Couldn't coerce value for field `template` ... Expected `record<task_status_template>` but found `NONE`". Same stale setup_statuses event as the E1002s above.

E2034  schema/suite/project/project.surql
    `template` must be set when creating a `task_status`
    verdict: genuine — engine-verified: `template` is `TYPE record<task_status_template>` with no DEFAULT, so the CREATE fails with "Couldn't coerce value for field `template` ... Expected `record<task_status_template>` but found `NONE`". Same stale setup_statuses event as the E1002s above.

E2034  schema/suite/project/project.surql
    `template` must be set when creating a `task_status`
    verdict: genuine — engine-verified: `template` is `TYPE record<task_status_template>` with no DEFAULT, so the CREATE fails with "Couldn't coerce value for field `template` ... Expected `record<task_status_template>` but found `NONE`". Same stale setup_statuses event as the E1002s above.

E2034  schema/suite/project/project.surql
    `template` must be set when creating a `task_status`
    verdict: genuine — engine-verified: `template` is `TYPE record<task_status_template>` with no DEFAULT, so the CREATE fails with "Couldn't coerce value for field `template` ... Expected `record<task_status_template>` but found `NONE`". Same stale setup_statuses event as the E1002s above.

E2034  schema/suite/project/project.surql
    `template` must be set when creating a `task_status`
    verdict: genuine — engine-verified: `template` is `TYPE record<task_status_template>` with no DEFAULT, so the CREATE fails with "Couldn't coerce value for field `template` ... Expected `record<task_status_template>` but found `NONE`". Same stale setup_statuses event as the E1002s above.

E5002  schema/auth/entity_access.surql
    argument 1 to `fn::entity::organization` is a `record<organization>`, but `$entity` is declared `record<project | calendar | outlet>`
    verdict: genuine — entity_access.surql:44, inside `SELECT VALUE id FROM employee_of WHERE ... AND out = fn::entity::organization(out)`. The bare `out` binds to the *subquery's* row, and `employee_of` is `TYPE RELATION FROM account TO organization`, so it is a `record<organization>`; the author meant the outer `entity_access.out` (the entity). Engine-verified on 3.0.5 that this is a hard failure, not a coercion: `DEFINE FUNCTION fn::takes($e: record<proj>)` called with `org:x` gives "Incorrect arguments for function fn::takes(). Failed to coerce argument `$e`: Expected `record<proj>` but found `org:x`". The whole FOR CREATE/UPDATE/DELETE permission therefore throws. Surfaced only after the comment-lowering fix (docs/plans/2026-07-26-oracle-triage.md, BUG-2).

E5002  schema/organization/billing/organization_billing.surql
    argument 1 to `fn::stripe::customer::delete` is a `none | string`, but `$stripe_id` is declared `string`
    verdict: genuine — engine-verified: `organization_billing.stripe` is `option<string>` and `fn::stripe::customer::delete($stripe_id: string)` rejects NONE ("Incorrect arguments ... Expected `string` but found `NONE`"). Deleting a billing row that never reached Stripe throws.

W7005  schema/@functions.surql
    `!=` between `record<organization_unit>` and `none` is always true
    verdict: genuine — `employee_of.unit` is declared `TYPE record<organization_unit>` (employee_of.surql:19-21), not `option<...>`, so `$grant.unit != NONE` at @functions.surql:23 is dead. Either the field should be optional or the guard should go.

W7005  schema/auth/entity_access.surql
    `!=` between `array<record<employee_of>, 1>` and `none` is always true
    verdict: genuine — entity_access.surql:40-47, `(SELECT VALUE id FROM employee_of WHERE ... LIMIT 1) != NONE`. A non-ONLY SELECT always yields an array, never NONE. Engine-verified on 3.0.5: with no matching row the subquery is `[]` and `[] != NONE` is `true`. So the "require active employment in the entity's Organization" half of the guard never denies anything; it needs `FROM ONLY ... LIMIT 1` (or `array::len(...) > 0`). Surfaced only after the comment-lowering fix (docs/plans/2026-07-26-oracle-triage.md, BUG-2).
