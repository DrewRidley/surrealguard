[package]
name = "surrealql-analyzer-wasm"
version = "0.1.0"
edition = "2021"
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "WebAssembly (wasm32-wasip1) bindings exposing the SurrealQL Analyzer engine to the browser playground"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
surrealql-analyzer-workspace = { path = "../workspace" }
surrealql-analyzer-diagnostics = { path = "../diagnostics" }
surrealql-analyzer-syntax = { path = "../syntax" }
surrealql-analyzer-embed = { path = "../embed" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# This crate is deliberately NOT wired into the workspace lint config; the
# native `cargo build --workspace` still builds it (as a host cdylib) but the
# real target is wasm32-wasip1. See crates/wasm/README.md for the build.

[lints]
workspace = true
